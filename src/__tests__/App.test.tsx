import React from "react";
import { render, fireEvent, waitForElement } from "@testing-library/react";

import App from "../../App";

describe("<App />", () => {
  test("should display a blank screen with some text", async () => {});
});
